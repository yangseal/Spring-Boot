function onPageLoad() {
	MessagePush.onPageLoad();
}
//推送信息
function showMessage(autoMessage) {
	alert(autoMessage);
	var t = JSON.parse(autoMessage);
	$("#DemoDiv").append(t.entityName).append("<br/>");
}